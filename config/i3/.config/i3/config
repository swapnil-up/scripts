# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 10

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Start XDG autostart .desktop files using dex. See also
# https://wiki.archlinux.org/index.php/XDG_Autostart
exec --no-startup-id dex --autostart --environment i3

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/github/scripts/scripts/i3lock-wrapper --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Have a bluetooth daemon
exec --no-startup-id blueman-applet

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && notify-send "Volume Up" "$(pactl get-sink-volume @DEFAULT_SINK@ | grep -oP '\d+%' | head -1)" && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && notify-send "Volume Down" "$(pactl get-sink-volume @DEFAULT_SINK@ | grep -oP '\d+%' | head -1)" && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && notify-send "Mute Toggle" "$(pactl get-sink-mute @DEFAULT_SINK@ | grep -oP '(yes|no)' | head -1)" && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
# bindsym $mod+d exec --no-startup-id dmenu_run
# A more modern dmenu replacement is rofi:
# bindcode $mod+40 exec "rofi -modi drun,run -show drun"
# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop
# bindsym $mod+d exec --no-startup-id rofi -show drun
bindsym $mod+d exec --no-startup-id ~/github/scripts/scripts/rofi/rofi-smart-launcher.sh
bindsym $mod+g exec --no-startup-id ~/github/scripts/scripts/rofi/fastfind-rofi.sh
bindsym $mod+Tab exec --no-startup-id rofi -show window

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
# bindsym $mod+Shift+v split h

# split in vertical orientation
# bindsym $mod+v split v

# Quickly toggle layout with 'space'
bindsym $mod+space layout toggle split

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
# bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+p floating toggle

# change focus between tiling / floating windows
bindsym $mod+p focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

assign [class="(?i)code"] $ws3
assign [class="(?i)firefox.*"] $ws4


# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
# bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"


# resize window (you can also use the mouse for that)
mode "resize" {
		bindsym h resize shrink width 10 px or 10 ppt
		bindsym j resize grow height 10 px or 10 ppt
		bindsym k resize shrink height 10 px or 10 ppt
		bindsym l resize grow width 10 px or 10 ppt


        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"

# --- Launch Mode (Apps) ---
set $mode_launch Launch: [f]irefox [c]ode [a]nki [n]autilus [m]pv [o]bsidian
mode "$mode_launch" {
    bindsym f exec --no-startup-id "command -v firefox-dev >/dev/null && firefox-dev || firefox", mode "default"
    bindsym c exec --no-startup-id code, mode "default"
    bindsym a exec --no-startup-id anki, mode "default"
    bindsym n exec --no-startup-id nautilus --no-desktop --new-window, mode "default"
    bindsym m exec --no-startup-id mpv --player-operation-mode=pseudo-gui, mode "default"
    bindsym o exec --no-startup-id obsidian, mode "default"

    # back to normal
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+z mode "$mode_launch"

# --- System Mode (Power) ---
set $mode_system System: [l]ock [e]xit [s]uspend [r]eboot [p]oweroff
mode "$mode_system" {
    bindsym l exec --no-startup-id ~/github/scripts/scripts/i3lock-wrapper, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id systemctl suspend, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym p exec --no-startup-id systemctl poweroff, mode "default"

    # back to normal
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+BackSpace mode "$mode_system"

# --- Alias Picker ---
bindsym $mod+x exec --no-startup-id ~/github/scripts/scripts/rofi/rofi-alias-picker.sh

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        status_command /home/swap/.local/bin/i3status-wrapper
}
# exec_always --no-startup-id ~/.config/polybar/launch.sh



## Screenshots in clipboards
bindsym Ctrl+Print exec --no-startup-id sh -c 'maim --format=png --select Pictures/$(date +%Y-%m-%d_%H-%M-%S).png && xclip -selection clipboard -t image/png -i Pictures/$(ls -t Pictures | head -n 1)'

bindsym Ctrl+$mod+Print exec --no-startup-id maim --format=png --window $(xdotool getactivewindow) | xclip -selection clipboard -t image/png
bindsym $mod+e exec --no-startup-id maim --format=png --select | xclip -selection clipboard -t image/png



# start a nautilus
# bindsym $mod+g exec --no-startup-id nautilus --no-desktop --new-window

# start anki 
# bindsym $mod+Shift+g exec --no-startup-id anki

# autostart anki
# exec --no-startup-id i3-msg 'exec --no-startup-id /usr/local/bin/anki'

# globally take a highlighted text and put it in anki
bindsym $mod+o exec --no-startup-id ~/github/scripts/scripts/anki/anki-piper.sh

# Make anki additions floating
for_window [class="Anki" title="Add"] floating enable, border pixel 5, sticky enable, focus
for_window [class="Anki" title="Edit"] floating enable, border pixel 5, focus
# Center so it doesn't cover your reading area
for_window [class="Anki" title="Add"] move position center

# connect controller
bindsym Mod1+Shift+b exec ~/connect_and_launch.sh

# start qutebrowser browser
#bindsym $mod+Shift+b exec --no-startup-id qutebrowser

# start Firefox browser
# bindsym $mod+b exec --no-startup-id "command -v firefox-dev >/dev/null && firefox-dev || firefox"

# Screen brightness controls
bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set +5% && notify-send "Brightness Up" "$(brightnessctl | grep -oP '\(\K\d+%' | head -1)"
bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 5%- && notify-send "Brightness Down" "$(brightnessctl | grep -oP '\(\K\d+%' | head -1)"
# Set brightness to bare minimum (e.g., 1%)
bindsym $mod+XF86MonBrightnessDown exec --no-startup-id brightnessctl set 1



# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# remove a floating window from scratchpad
# bindsym $mod+x floating disable; floating enable

#custom start script
exec --no-startup-id sleep 5 && ~/startup.sh

#Adding a desktop background 
# exec --no-startup-id nitrogen --restore
exec_always --no-startup-id feh --bg-center ~/possessions/Pictures/desktop-wallpapers/a_dock_on_the_water.jpeg

# Change wallpaper with a shortcut 
# bindsym $mod+Shift+w exec --no-startup-id ~/.config/i3/change_wallpaper.sh


# Starting and always having obsidian in scratchpad
# exec --no-startup-id obsidian
# for_window [class="obsidian"] move to scratchpad

# Adding floats for extra types
for_window [window_role="pop-up"] floating enable
for_window [window_type="dialog"] floating enable
for_window [window_type="utility"] floating enable


# Obsidian quick key shortcut
# bindsym $mod+o exec --no-startup-id obsidian

# Starting chatgpt at the start
# exec --no-startup-id sleep 5 && chatgpt-desktop

# dunst and notifications
exec --no-startup-id dunst
exec --no-startup-id ~/github/scripts/scripts/battery-notifier.sh
exec --no-startup-id ~/github/scripts/scripts/resource-monitor.sh

#clipboard
exec --no-startup-id CM_SELECTIONS=clipboard clipmenud
# bindsym $mod+c exec --no-startup-id clipmenu
# exec --no-startup-id cliphist daemon
# bindsym $mod+c exec --no-startup-id bash -c 'cliphist list | rofi -dmenu | cliphist decode | xclip -selection clipboard'
bindsym $mod+v exec --no-startup-id env CM_SELECTIONS=clipboard CM_LAUNCHER=rofi clipmenu -i -p "Clipboard History:"

# tap to click touchpad
exec --no-startup-id bash -c 'sleep 1; xinput set-prop "$(xinput list --name-only | grep -i touchpad | head -n1)" "libinput Tapping Enabled" 1'

#remove window headers
for_window [class="^.*"] border pixel 2
                          # class                 border  backgr. text    indicator child_border
for_window [class="^.*"] client.focused          #77dd77 #285577 #ffffff #2e9ef4   #285577


# Multiple Screens
bindsym $mod+Shift+greater move container to output right
bindsym $mod+Shift+less move container to output left

# Focus the monitor to the left/right
bindsym $mod+greater focus output right
bindsym $mod+less focus output left

# Move the current workspace to the monitor on the right/left
bindsym $mod+Control+greater move workspace to output right
bindsym $mod+Control+less move workspace to output left

# Scratchpad window for quick notes
for_window [class="^quicknote_nvim$"] floating enable, sticky enable, border pixel 1, resize set 800 600, move position center, scratchpad show

# Bind a key to toggle the scratchpad
bindsym $mod+n exec --no-startup-id ~/.local/bin/quicknote.sh

# Toggle whisper recording (tap on left Alt via kanata -> f16 -> XF86Launch7)
bindsym XF86Launch7 exec --no-startup-id ~/github/scripts/scripts/utils/whisper-toggle.sh

# conky
# exec_always --no-startup-id conky -c ~/.config/conky/conky.conf

exec_always --no-startup-id conky -c ~/.config/conky/conky_projects.conf
exec_always --no-startup-id conky -c ~/.config/conky/conky_todo.conf
exec_always --no-startup-id conky -c ~/.config/conky/conky_stats.conf
exec_always --no-startup-id conky -c ~/.config/conky/conky_notes.conf
exec_always --no-startup-id conky -c ~/.config/conky/conky_year.conf
exec_always --no-startup-id conky -c ~/.config/conky/conky_quotes.conf



# exec_always --no-startup-id i3blocks -c ~/.config/i3blocks/config

#bar {
#    status_command i3blocks -c ~/.config/i3blocks/config
#    position top
#    font pango:JetBrainsMono Nerd Font 11
#    colors {
#        background #00000000
#        statusline #D8DEE9
#        separator  #4C566A
#    }
#}

# bindsym $mod+t exec --no-startup-id ~/rofi/rofi-todo-menu.sh
# Or individual bindings:
# bindsym $mod+t exec --no-startup-id ~/github/scripts/scripts/rofi-todo-add.sh
# bindsym $mod+shift+t exec --no-startup-id ~/github/scripts/scripts/rofi-todo-toggle.sh
# bindsym $mod+ctrl+t exec --no-startup-id ~/github/scripts/scripts/rofi-todo-remove.sh

# Open VS Code
# bindsym $mod+c exec --no-startup-id code

# Open mpv in a floating window
 for_window [class="mpv"] floating enable
# bindsym $mod+y exec --no-startup-id mpv --player-operation-mode=pseudo-gui

# snippet picker
for_window [title="Snippet Picker"] floating enable, resize set 1000 600, move position center

# alias picker
for_window [title="^Alias$"] floating enable, border pixel 1, resize set 800 600, move position center

# Set GTK dark mode on startup
exec --no-startup-id gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'
exec --no-startup-id gsettings set org.gnome.desktop.interface gtk-theme 'Adwaita-dark'

# picom for dimming non-active windows
exec_always --no-startup-id picom --config ~/.config/picom/picom.conf